name: CI file
run-name: ${{ github.actor }} is running CI
on:
  push:
    branches:
      - 'feat/**'
      - 'bug/**'
jobs:
    prepare:
        uses: ./.github/workflows/prepare.yaml
        with:
            commit_ref: ${{ github.ref }}
    unittest:
        uses: ./.github/workflows/unittest.yaml
    build:
        uses: ./.github/workflows/build.ymal 